# Anchors pytest's rootdir at tests/ - the package root has an __init__.py and
# must not be collected as a test package. Run from the package directory:
#     pytest tests/test_simulation_api.py -v
[pytest]
addopts = --import-mode=importlib -p no:cacheprovider
testpaths = .
